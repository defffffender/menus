# Прямые зависимости проекта Menus.
# Установка: pip install -r requirements.txt
Django==5.2.15
pillow==12.2.0
qrcode==8.2

# Конфиг из окружения (12-factor)
django-environ==0.14.0

# HTTP-клиент для интеграций (Eskiz SMS)
requests==2.32.3

# Realtime (WebSocket) — Channels + ASGI-сервер
channels==4.3.2
daphne==4.2.2
channels-redis==4.3.0   # channel layer для прода (dev — InMemory)

# PostgreSQL на проде (по умолчанию SQLite; включается через DATABASE_URL)
psycopg[binary]==3.2.10
